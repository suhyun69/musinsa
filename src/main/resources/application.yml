spring:
  jpa:
    show-sql: true # ?? ?? ??
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # ??? ?? ?? data.sql ??
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true